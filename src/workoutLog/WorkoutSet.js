import React, { } from "react";
import {  } from "@material-ui/core";

const WorkoutSet = (props) => {
  
  return (
    <div> 
        reps - weight 추가 페이지
        {/* 
            추가 버튼으로 reps/weight 한칸씩 추가
         */}
    </div>
  );
};

export default WorkoutSet;
