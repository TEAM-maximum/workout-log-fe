import React, { useState } from "react";
import {
  ListItem,
  ListItemText,
  InputBase,
  Checkbox,
  ListItemSecondaryAction,
  IconButton,
} from "@material-ui/core";

const WorkoutLog = (props) => {
    const [state, setState] = useState(0);
    return <div>workoutLog Test</div>
};

export default WorkoutLog;
